{
  "name": "ecommerce-ai-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix frontend-user && npm install --prefix admin-site",
    "dev-server": "npm run dev --prefix server",
    "dev-user": "npm run dev --prefix frontend-user",
    "dev-admin": "npm run dev --prefix admin-site",
    "seed": "npm run seed --prefix server"
  }
}
